// Defining a namespace `Subjects` to group related interfaces or classes
namespace Subjects {
  
  // Exporting the `Teacher` interface so it can be accessed outside the `Subjects` namespace
  export interface Teacher {
    firstName: string;  // The first name of the teacher (string type)
    lastName: string;   // The last name of the teacher (string type)
  }
}

